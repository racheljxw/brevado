# Dev-only dependencies, on top of requirements.txt — not installed on Render (the
# deployed service never runs tests). Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
